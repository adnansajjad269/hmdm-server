# Grafana file-provisioned notification template — static (no envsubst substitution
# needed), installed as-is by install.sh. Referenced by the "hmdm-offline-report-webhook"
# contact point's settings.payload.template field (a full custom-payload webhook, not
# the built-in "message" field) as {{ template "hmdm_offline_report" . }} -- this produces
# the ENTIRE raw JSON body sent, e.g. Google Chat's {"text": "..."} shape, with nothing
# added by Grafana (no title, no "Open in Grafana" button, no version footer).
#
# device_list is built in SQL with a "~~~" delimiter, NOT a real newline -- Grafana's
# alerting pipeline appears to drop/blank an entire alert instance's labels when one of
# them contains a raw newline (this is the likely cause of an earlier attempt where the
# whole message rendered blank). The delimiter is converted back to a real newline only
# here, inside the final string, right before it's JSON-encoded -- never as a label
# value. data.ToJSON handles all JSON string escaping (quotes, newlines, etc.), so the
# output is always valid JSON regardless of what device/owner names contain.
#
# Deliberately uses only range/index/dot-field access plus the documented reReplaceAll
# and data.ToJSON functions -- no Sprig-style helpers (dict/list/append/add), since
# Grafana's alerting template engine doesn't guarantee those are available (and Go's
# text/template has no arithmetic operators at all, so counting must stay in SQL). All
# counting/bucketing/formatting/ordering already happened in the "Offline devices
# report" rule's SQL (see rules.yaml.tmpl); this template only assembles and JSON-encodes
# the already-formatted rows.
#
# Because heading/device_list embed the per-device hours-offline count, their label
# VALUES change on essentially every evaluation as that count drifts -- and since label
# values are part of Grafana's alert instance identity (fingerprint), each change spins
# up a brand-new instance while the previous one lingers "Firing" for a few more
# evaluation cycles until it resolves. That overlap window means .Alerts.Firing can
# legitimately contain two (or more) instances for the SAME tier at once, differing only
# by a now-stale hour count -- seen in practice as e.g. "Offline 1-12 hours" appearing
# twice in one message. Rather than a full label/identity redesign, each of the three
# explicit per-tier passes below takes only the FIRST matching instance and ignores any
# further duplicate for that same tier, which also gives us the required gt12h / lt12h /
# lt1h ordering for free.
apiVersion: 1

templates:
  - orgId: 1
    name: hmdm_offline_report
    template: |
      {{ define "hmdm_offline_report" }}
      {{- $total := "" -}}
      {{- if gt (len .Alerts.Firing) 0 -}}
      {{- $total = (index .Alerts.Firing 0).Labels.total_offline -}}
      {{- end -}}
      {{- $msg := printf "Offline Devices Report -- %s devices offline\n" $total -}}
      {{- $doneGt12h := false -}}
      {{- range .Alerts.Firing -}}
      {{- if and (eq .Labels.tier "gt12h") (not $doneGt12h) -}}
      {{- $msg = printf "%s\n------------------------------------\n%s\n------------------------------------\n%s\n" $msg .Labels.heading (reReplaceAll "~~~" "\n" .Labels.device_list) -}}
      {{- $doneGt12h = true -}}
      {{- end -}}
      {{- end -}}
      {{- $doneLt12h := false -}}
      {{- range .Alerts.Firing -}}
      {{- if and (eq .Labels.tier "lt12h") (not $doneLt12h) -}}
      {{- $msg = printf "%s\n------------------------------------\n%s\n------------------------------------\n%s\n" $msg .Labels.heading (reReplaceAll "~~~" "\n" .Labels.device_list) -}}
      {{- $doneLt12h = true -}}
      {{- end -}}
      {{- end -}}
      {{- $doneLt1h := false -}}
      {{- range .Alerts.Firing -}}
      {{- if and (eq .Labels.tier "lt1h") (not $doneLt1h) -}}
      {{- $msg = printf "%s\n------------------------------------\n%s\n------------------------------------\n%s\n" $msg .Labels.heading (reReplaceAll "~~~" "\n" .Labels.device_list) -}}
      {{- $doneLt1h = true -}}
      {{- end -}}
      {{- end -}}
      {"text": {{ data.ToJSON $msg }}}
      {{ end }}
